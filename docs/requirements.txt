# Documentation build dependencies.
#
# Sphinx + theme, plus the kpfpipe *runtime* dependencies. kpfpipe itself is
# installed separately, by the post_install job in .readthedocs.yaml (which
# waives the exact "==3.14.3" interpreter pin), so it is deliberately absent
# here. Its real third-party libraries must be present for autodoc's imports to
# succeed — mocking them is too fragile for a package this size (module-level
# astropy-unit math, importlib.metadata version lookups, etc. all run at import
# time).
#
# The versions below MIRROR pyproject.toml [project].dependencies and must be
# kept in sync with it (update both together).

# --- docs tooling ---
sphinx
sphinx-rtd-theme
myst-parser  # render the Markdown governing docs under source/guidelines/

# --- kpfpipe runtime deps (mirror of pyproject.toml [project].dependencies) ---
astropy==7.2.0
astroquery==0.4.11
barycorrpy==0.4.4
h5py==3.15.1
matplotlib==3.10.8
numpy==2.4.2
pandas==3.0.0
pillow==12.1.1
scipy==1.17.0
tqdm==4.67.3
rv-data-standard==0.4.0
