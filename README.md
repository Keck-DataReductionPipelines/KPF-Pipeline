# KPF-Pipeline
Data Reduction Pipeline for the Keck Planet Finder spectrograph


To run the pipeline, clone this repository to your local machine (or pip install in developer mode).

Generate a conda environment from environment.yaml. This will create an environment named `kpfpipe`
